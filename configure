#!/bin/sh
. ./tconf.sh

tconf_init "$@"

tconf_search_cc "$HOST"
tconf_require "C compiler" "$CC"
tconf_find_os

tconf_check_func "$CC" "stdlib.h" "qsort"
tconf_check_func "$CC" "signal.h" "signal"

tconf_fini
